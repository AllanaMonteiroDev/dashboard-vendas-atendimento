Dashboard Web de Vendas e Atendimento

Painel que mostra indicadores de vendas e atendimento, usando banco SQL, Python e uma página web.

Projeto do Tech Girls Challenge — Equipe Manhã.

Equipe
Amanda — Frontend
Allana da Cruz — Backend, dados e SQL
Stack

SQLite, Python (pandas), HTML, CSS

Base de dados

Amazon Sales Dataset (Kaggle) — dados de produtos, preços, descontos e avaliações de clientes.

Baixe o arquivo amazon.csv do Kaggle e coloque na raiz do projeto (mesma pasta do backend.py) antes de rodar.

Como rodar
bash
# 1. Clonar o repositório
git clone <link-do-repositorio>
cd <nome-da-pasta>

# 2. Criar e ativar o ambiente virtual
python -m venv venv
venv\Scripts\activate       # Windows
source venv/bin/activate    # Mac/Linux

# 3. Instalar as dependências
pip install pandas

# 4. Colocar o arquivo amazon.csv na pasta do projeto (baixado do Kaggle)

# 5. Rodar o script (gera o banco.db e o dados.json)
python backend.py

# 6. Subir um servidor local e abrir o painel
python -m http.server
# depois acessar http://localhost:8000 no navegador

O que o backend.py faz: lê o amazon.csv, trata valores nulos/inválidos, converte colunas de texto (preço e desconto) para número, salva os dados tratados num banco SQLite (banco.db, tabela produtos) e calcula os 5 indicadores, gerando o arquivo dados.json para o frontend consumir.

Indicadores
Preço médio com desconto — média do valor final pago pelo cliente (coluna discounted_price).
Percentual médio de desconto — média do desconto aplicado em relação ao preço original (coluna discount_percentage).
Avaliação média — nota média de 1 a 5 dada pelos clientes (coluna rating).
Volume de avaliações — soma do total de avaliações recebidas, indicando popularidade (coluna rating_count).
Distribuição por categoria principal — contagem de produtos por categoria, usando apenas o primeiro nível da coluna category (ela vem com o caminho completo, tipo "Computers&Accessories|Accessories&Peripherals|Cables", então é tratada para pegar só a parte antes do primeiro "|").
Status
 Base de dados definida (Amazon Sales Dataset)
 Indicadores definidos
 Banco SQL pronto (SQLite, tabela produtos)
 Script Python pronto (backend.py gera banco.db e dados.json)
 Tratamento de erros testado (arquivo ausente)
 Página web pronta
 Gráfico pronto
